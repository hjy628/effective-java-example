Java语言支持四种类型:
接口(interface)、类(class)、数组(array)、基本类型(primitive)
前三种类型通常被称为引用类型(reference type),类实例和数组是对象，而基本类型的值不是对象
类的成员由它的域(field)、方法(method)、成员类(member class)和成员接口(member interface)组成
方法的签名(signature)由它的名称和所有参数类型组成；签名不包括它的返回类型。